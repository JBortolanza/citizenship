localhost {
    tls internal

    handle_path /api* {
        reverse_proxy backend:8000
    }

    handle {
        root * /srv/frontend/dist
        file_server
        try_files {path} /index.html
    }
}