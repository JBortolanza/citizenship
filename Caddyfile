localhost {
    tls internal

    handle_path /api* {
        reverse_proxy backend:8000
    }

    # Proxy all other requests to the Vite dev server on your host
    handle {
        reverse_proxy host.docker.internal:5173
    }
}